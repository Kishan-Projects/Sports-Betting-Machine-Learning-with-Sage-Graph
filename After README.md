For the best outputs and a easier streamline experience is to use the code on google Colab, and download the data file (nfl.xl) into you google drive.
The change your Hardware accelerator in the runtime type settings from the code to use only GPU (either T4 GPU or A100 GPU) instead of CPU. 
And also have the latest runtime version, which is in the same loaction as the GPU settings, if the code does not work, just switch the version to - 2025.10 
